interface:
  display_name: "EMP Checklist"
  short_description: "Repo-specific patterns and workflows for EMP-Checklist"
  default_prompt: "Use the EMP-Checklist repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true